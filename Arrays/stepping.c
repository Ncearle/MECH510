#include <stdio.h>
#include <stdlib.h>

//int main() {
//  double time, dt, finalTime = 1.;
//  int timesteps = 19, step;
//  dt = finalTime / timesteps;
//  printf("Target number of steps: %d\n", timesteps);
//  for (time = 0, step = 1; time < finalTime; time += dt, step++) {
//    printf("Step %d: %.17f\n", step, time);
//  }
//  exit(0);
//}